/**
 * Defines all paths for both the server and client
 */
const paths = {

    index: "/",
    speak: "/speak",

    conversations : {
        add: "/conversations/add",
        delete: "/conversations/delete",
        save: "/conversations/save",
        get: "/conversations"
    }
};

module.exports = paths;